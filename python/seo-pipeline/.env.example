# Copy this file to .env and fill in your values

# Required
ANTHROPIC_API_KEY=xyz

# Optional — only needed if you choose SEMrush during pipeline run
SEMRUSH_API_KEY=your_semrush_api_key_here

# Google APIs are configured separately via:
#   python scripts/google_auth.py --setup
# This creates ~/.config/claude-seo/google-api.json
